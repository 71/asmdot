{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run current file",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "env": {
        "PYTHONPATH": "${workspaceRoot}"
      }
    }
  ]
}
